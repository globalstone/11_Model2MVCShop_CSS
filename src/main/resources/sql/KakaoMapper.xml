<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="KakaoMapper">

    <!-- 정보 찾기 -->
    <select id="findKakao" parameterType="java.util.HashMap" resultType="kakao">
        select * from kakao_table where k_name=#{K_NAME, jdbcType=VARCHAR} and k_email=#{email, jdbcType=VARCHAR}
    </select>

    <!-- 정보 저장 -->
    <insert id="kakaoInsert" parameterType="java.util.HashMap">
        insert into kakao_table(k_name, k_email)
        values(#{K_NAME, jdbcType=VARCHAR, javaType=java.lang.String, typeHandler=org.apache.ibatis.type.StringTypeHandler}, #{email, jdbcType=VARCHAR})
    </insert>

</mapper>
